<div class="card">
  <% if prototype.image.present? %>
    <%= link_to image_tag(prototype.image, class: :card__img), prototype_path(prototype) %>
  <% else %>
    <%#= link_to image_tag(  class: :card__img), prototype_path(prototype) %>
  <% end %>
   <%#= link_to prototype_path(prototype) do %>

  <div class="card__body">
    <%#<%= prototype.image %>
    <%#<%= image_tag prototype.image, class: 'prototype-image' if prototype.image.attached? %>
   <%= link_to prototype.title, prototype_path(prototype), class: :card__title %>
    <%#= prototype.title %>

    <p class="card__summary">
      <%= prototype.catch_copy %>
    </p>
    
  <%= link_to "by #{prototype.user.name}", user_path(prototype.user), class: :card__user %>

  </div>
</div>